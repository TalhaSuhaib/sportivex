<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sportivex - Premium Sports Gear</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
</head>
<body class="bg-gray-950 text-gray-100 font-sans min-h-screen flex flex-col justify-between">

    <!-- Header / Navbar -->
    <nav class="bg-gray-900 border-b border-gray-800 sticky top-0 z-50 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
            
            <!-- Dynamic Logo & Profile Photo -->
            <div class="flex items-center space-x-3 cursor-pointer" onclick="filterCategory('all')">
                <img id="site-profile-img" src="" class="w-10 h-10 rounded-full object-cover border-2 border-yellow-400 hidden">
                <div class="flex items-center space-x-2">
                    <i id="site-logo-icon" class="fa-solid fa-bolt text-yellow-400 text-2xl"></i>
                    <span id="site-logo-text" class="text-2xl font-black text-white tracking-wide">SPORT<span class="text-yellow-400">IVEX</span></span>
                </div>
            </div>
            
            <div class="flex items-center space-x-3">
                <!-- Auth Section -->
                <div id="auth-section"></div>

                <!-- Admin Panel Button -->
                <button id="admin-btn" onclick="openAdminModal()" class="hidden bg-yellow-400 text-gray-950 font-bold px-3 py-1.5 rounded-lg hover:bg-yellow-500 text-sm transition flex items-center gap-1.5 shadow-lg shadow-yellow-400/20">
                    <i class="fa-solid fa-sliders"></i> Admin Panel
                </button>

                <!-- Cart Button -->
                <button onclick="toggleCart()" class="relative bg-gray-800 p-2.5 rounded-lg hover:bg-gray-700 border border-gray-700 transition">
                    <i class="fa-solid fa-cart-shopping text-xl text-yellow-400"></i>
                    <span id="cart-count" class="absolute -top-2 -right-2 bg-red-600 text-white text-xs px-2 py-0.5 rounded-full font-bold">0</span>
                </button>
            </div>
        </div>
    </nav>

    <!-- Dynamic Hero Banner -->
    <div id="hero-banner" class="relative bg-cover bg-center py-16 sm:py-24 px-4 text-center border-b border-gray-800 transition-all duration-300">
        <div class="max-w-4xl mx-auto space-y-4">
            <h1 id="banner-title" class="text-3xl sm:text-5xl font-black text-white tracking-tight uppercase">SPORTIVEX - PREMIUM GEAR</h1>
            <p id="banner-subtitle" class="text-base sm:text-lg text-gray-300 max-w-2xl mx-auto">Get authentic jerseys, boots, shorts, and sports accessories delivered nationwide.</p>
        </div>
    </div>

    <!-- Dynamic Category / Segment Bar -->
    <div class="bg-gray-900/90 backdrop-blur border-b border-gray-800 py-3 sticky top-[61px] z-40">
        <div class="max-w-7xl mx-auto px-4 flex gap-2 overflow-x-auto no-scrollbar" id="category-container"></div>
    </div>

    <!-- Products Section -->
    <main class="max-w-7xl mx-auto px-4 py-8 flex-grow w-full">
        <div class="flex justify-between items-center mb-6">
            <h2 id="current-category-title" class="text-2xl font-bold text-yellow-400">All Products</h2>
            <span id="product-count-tag" class="text-xs bg-gray-800 border border-gray-700 px-3 py-1 rounded-full text-gray-400">0 Products</span>
        </div>
        <div id="product-grid" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6"></div>
    </main>

    <!-- Cart Drawer Modal -->
    <div id="cart-modal" class="fixed inset-0 bg-black/80 z-50 hidden flex justify-end backdrop-blur-sm">
        <div class="bg-gray-900 w-full max-w-md h-full p-6 flex flex-col justify-between overflow-y-auto border-l border-gray-800">
            <div>
                <div class="flex justify-between items-center border-b border-gray-800 pb-4 mb-4">
                    <h3 class="text-xl font-bold text-yellow-400 flex items-center gap-2"><i class="fa-solid fa-cart-shopping"></i> Shopping Cart</h3>
                    <button onclick="toggleCart()" class="text-gray-400 hover:text-white text-xl"><i class="fa-solid fa-xmark"></i></button>
                </div>
                
                <div id="cart-items" class="space-y-4 max-h-[35vh] overflow-y-auto pr-2"></div>

                <div class="border-t border-gray-800 mt-4 pt-4">
                    <div class="flex justify-between font-bold text-lg mb-4">
                        <span>Total Payable:</span>
                        <span id="cart-total" class="text-yellow-400">৳0</span>
                    </div>

                    <form id="checkout-form" onsubmit="handleCheckout(event)" class="space-y-3">
                        <h4 class="font-bold text-gray-300 border-b border-gray-800 pb-1 text-xs uppercase">Customer Info</h4>
                        <input type="text" id="cust-name" placeholder="Full Name *" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm focus:outline-none focus:border-yellow-400">
                        <input type="tel" id="cust-phone" placeholder="Phone Number *" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm focus:outline-none focus:border-yellow-400">
                        <textarea id="cust-address" placeholder="Delivery Address *" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm focus:outline-none focus:border-yellow-400 h-16"></textarea>

                        <h4 class="font-bold text-gray-300 border-b border-gray-800 pb-1 text-xs uppercase pt-2">Payment Option</h4>
                        <div class="space-y-2">
                            <label class="flex items-center space-x-2 border border-gray-800 p-2.5 rounded cursor-pointer bg-gray-800/50">
                                <input type="radio" name="payment-method" value="COD" checked onclick="togglePaymentDetails()" class="accent-yellow-400">
                                <span class="text-sm">Cash on Delivery (COD)</span>
                            </label>
                            <label class="flex items-center space-x-2 border border-gray-800 p-2.5 rounded cursor-pointer bg-gray-800/50">
                                <input type="radio" name="payment-method" value="bKash" onclick="togglePaymentDetails()" class="accent-yellow-400">
                                <span class="text-sm">bKash (Send Money)</span>
                            </label>
                        </div>

                        <div id="bkash-info" class="hidden bg-pink-950/40 border border-pink-700/50 p-3 rounded text-xs space-y-2">
                            <p class="text-pink-300 font-semibold">Send Money to bKash Number:</p>
                            <p id="display-bkash-no" class="text-yellow-400 font-bold text-base bg-gray-950 p-1.5 rounded text-center tracking-widest border border-pink-900">01717488371</p>
                            <input type="text" id="bkash-trx" placeholder="Enter bKash TrxID *" class="w-full bg-gray-800 border border-gray-700 rounded p-2 text-sm text-white focus:outline-none">
                        </div>

                        <button type="submit" class="w-full bg-green-600 hover:bg-green-700 font-bold py-3 rounded-lg text-white transition flex items-center justify-center gap-2 mt-4 shadow-lg shadow-green-900/30">
                            <i class="fa-brands fa-whatsapp text-xl"></i> Confirm Order via WhatsApp
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- MASTER ADMIN CMS MODAL -->
    <div id="admin-modal" class="fixed inset-0 bg-black/90 z-50 hidden flex justify-center items-center p-2 sm:p-4 backdrop-blur-md">
        <div class="bg-gray-900 w-full max-w-5xl h-[95vh] rounded-2xl border border-gray-800 p-4 sm:p-6 flex flex-col justify-between shadow-2xl overflow-hidden">
            
            <!-- Modal Header -->
            <div class="flex justify-between items-center border-b border-gray-800 pb-4">
                <div>
                    <h3 class="text-xl sm:text-2xl font-black text-yellow-400 flex items-center gap-2"><i class="fa-solid fa-sliders"></i> Sportivex Admin Control Center</h3>
                    <p class="text-xs text-gray-400">Logged in Admin: <span id="admin-logged-email" class="text-green-400 font-semibold">...</span></p>
                </div>
                <button onclick="closeAdminModal()" class="text-gray-400 hover:text-white text-2xl"><i class="fa-solid fa-xmark"></i></button>
            </div>

            <!-- CMS Nav Tabs -->
            <div class="flex border-b border-gray-800 my-3 gap-2 overflow-x-auto no-scrollbar">
                <button onclick="switchAdminTab('site-settings')" id="tab-btn-settings" class="py-2 px-3 text-sm font-bold border-b-2 border-yellow-400 text-yellow-400 whitespace-nowrap">Site Branding & Banner</button>
                <button onclick="switchAdminTab('add-product')" id="tab-btn-add" class="py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap">Add Product & Offers</button>
                <button onclick="switchAdminTab('manage-products')" id="tab-btn-manage" class="py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap">Manage Products</button>
                <button onclick="switchAdminTab('manage-segments')" id="tab-btn-segments" class="py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap">Segments/Categories</button>
                <button onclick="switchAdminTab('manage-admins')" id="tab-btn-admins" class="py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap">Admin Emails</button>
                <button onclick="switchAdminTab('view-orders')" id="tab-btn-orders" class="py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap">Orders History</button>
            </div>

            <!-- CMS Main Content Body -->
            <div class="flex-grow overflow-y-auto pr-2 py-2">
                
                <!-- Tab 1: Branding & Banner Settings -->
                <div id="tab-site-settings" class="space-y-4">
                    <form onsubmit="handleSaveSiteSettings(event)" class="space-y-4">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Logo Name HTML</label>
                                <input type="text" id="cms-logo-text" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">WhatsApp Receiver Number</label>
                                <input type="text" id="cms-whatsapp-no" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                        </div>

                        <!-- Direct Profile Picture Upload -->
                        <div class="bg-gray-800/60 p-4 rounded-lg border border-gray-700">
                            <label class="block text-xs font-bold text-yellow-400 uppercase mb-2">Upload Profile Photo / Logo Badge (Optional File)</label>
                            <input type="file" id="cms-profile-file" accept="image/*" class="text-xs text-gray-300 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-semibold file:bg-yellow-400 file:text-gray-950 hover:file:bg-yellow-500">
                        </div>

                        <!-- Direct Banner Upload -->
                        <div class="bg-gray-800/60 p-4 rounded-lg border border-gray-700 space-y-3">
                            <label class="block text-xs font-bold text-yellow-400 uppercase">Main Banner Background Image (Upload File)</label>
                            <input type="file" id="cms-banner-file" accept="image/*" class="text-xs text-gray-300 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-semibold file:bg-yellow-400 file:text-gray-950 hover:file:bg-yellow-500">
                            
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Banner Main Title</label>
                                <input type="text" id="cms-banner-title" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Banner Subtitle / Description</label>
                                <input type="text" id="cms-banner-subtitle" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                        </div>

                        <button type="submit" class="w-full bg-yellow-400 hover:bg-yellow-500 text-gray-950 font-bold py-3 rounded-lg transition shadow-lg">Save Branding Live</button>
                    </form>
                </div>

                <!-- Tab 2: Add Product & Offer -->
                <div id="tab-add-product" class="hidden space-y-4">
                    <form onsubmit="handleAddProduct(event)" class="space-y-4">
                        <div>
                            <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Product Title / Name *</label>
                            <input type="text" id="prod-name" required placeholder="e.g. Real Madrid Home Jersey" class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                        </div>

                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Regular Price (৳) *</label>
                                <input type="number" id="prod-price" required placeholder="1200" class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-yellow-400 uppercase mb-1">Offer Price (৳) (Optional)</label>
                                <input type="number" id="prod-offer" placeholder="950" class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                        </div>

                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                            <div>
                                <label class="block text-xs font-bold text-gray-300 uppercase mb-1">Category Segment *</label>
                                <select id="prod-category" required class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm text-white">
                                </select>
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-yellow-400 uppercase mb-1">Offer Badge Tag (Optional)</label>
                                <input type="text" id="prod-tag" placeholder="e.g. 20% OFF or HOT DEAL" class="w-full bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                            </div>
                        </div>

                        <div class="bg-gray-800/60 p-4 rounded-lg border border-gray-700">
                            <label class="block text-xs font-bold text-yellow-400 uppercase mb-2">Upload Product Image (Direct File) *</label>
                            <input type="file" id="prod-image-file" accept="image/*" required class="text-xs text-gray-300 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-xs file:font-semibold file:bg-yellow-400 file:text-gray-950 hover:file:bg-yellow-500">
                        </div>

                        <button type="submit" class="w-full bg-yellow-400 hover:bg-yellow-500 text-gray-950 font-bold py-3 rounded-lg transition shadow-lg">Publish Product Now</button>
                    </form>
                </div>

                <!-- Tab 3: Manage Existing Products -->
                <div id="tab-manage-products" class="hidden space-y-4">
                    <div id="admin-product-list" class="divide-y divide-gray-800"></div>
                </div>

                <!-- Tab 4: Segments/Categories Management -->
                <div id="tab-manage-segments" class="hidden space-y-4">
                    <form onsubmit="handleAddSegment(event)" class="grid grid-cols-1 sm:grid-cols-3 gap-2">
                        <input type="text" id="seg-id" placeholder="Segment ID (e.g. gloves) *" required class="bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                        <input type="text" id="seg-name" placeholder="Display Name (e.g. Gloves) *" required class="bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                        <button type="submit" class="bg-yellow-400 hover:bg-yellow-500 text-gray-950 font-bold px-4 py-2 rounded-lg text-sm">Add Category</button>
                    </form>
                    <h4 class="font-bold text-gray-300 text-sm border-b border-gray-800 pb-2">Active Categories List</h4>
                    <div id="admin-segments-list" class="space-y-2"></div>
                </div>

                <!-- Tab 5: Admin Email Access Control -->
                <div id="tab-manage-admins" class="hidden space-y-4">
                    <form onsubmit="handleAddAdminEmail(event)" class="flex gap-2">
                        <input type="email" id="new-admin-email" placeholder="Add Admin Gmail Address *" required class="flex-grow bg-gray-800 border border-gray-700 rounded p-2.5 text-sm">
                        <button type="submit" class="bg-green-600 hover:bg-green-700 text-white font-bold px-4 py-2 rounded-lg text-sm">Add Admin</button>
                    </form>
                    <h4 class="font-bold text-gray-300 text-sm border-b border-gray-800 pb-2">Allowed Admin Email Accounts</h4>
                    <div id="admin-emails-list" class="space-y-2"></div>
                </div>

                <!-- Tab 6: View Customer Orders -->
                <div id="tab-view-orders" class="hidden space-y-4">
                    <div id="admin-order-list" class="space-y-4"></div>
                </div>

            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-900 border-t border-gray-800 py-6 text-center text-gray-400 text-sm">
        <p>&copy; 2026 <span id="footer-logo">Sportivex</span>. All rights reserved.</p>
        <p class="mt-1 text-xs text-gray-500">WhatsApp Support: <span id="footer-wa-no">01717488371</span></p>
    </footer>

    <!-- JavaScript Application Core -->
    <script>
        let currentUser = JSON.parse(localStorage.getItem('sz_current_user')) || null;

        const defaultSettings = {
            logoText: "SPORT<span class='text-yellow-400'>IVEX</span>",
            logoIcon: "fa-solid fa-bolt",
            profileImg: "",
            whatsappNo: "01717488371",
            bannerTitle: "SPORTIVEX - PREMIUM GEAR",
            bannerSubtitle: "Get authentic jerseys, boots, shorts, and sports accessories delivered nationwide.",
            bannerBg: "https://images.unsplash.com/photo-1508098682722-e99c43a406b2?w=1200"
        };

        const defaultAdminEmails = ["talhasuhaib53@gmail.com", "admin@sportivex.com"];

        const defaultCategories = [
            { id: "all", name: "All Products" },
            { id: "jersey", name: "Jersey" },
            { id: "shorts", name: "Shorts" },
            { id: "boots", name: "Boots" },
            { id: "accessories", name: "Accessories" }
        ];

        const defaultProducts = [
            { id: 1, name: "Real Madrid Home Jersey 24/25", price: 1200, offerPrice: 950, tag: "20% OFF", category: "jersey", image: "https://images.unsplash.com/photo-1518091043644-c1d4457512c6?w=500" },
            { id: 2, name: "Nike Mercurial Vapor Boots", price: 4500, offerPrice: 3900, tag: "HOT SALE", category: "boots", image: "https://images.unsplash.com/photo-1511886929837-354d827aae26?w=500" }
        ];

        let siteSettings = JSON.parse(localStorage.getItem('sz_settings')) || defaultSettings;
        let adminEmails = JSON.parse(localStorage.getItem('sz_admin_emails')) || defaultAdminEmails;
        let categories = JSON.parse(localStorage.getItem('sz_categories')) || defaultCategories;
        let products = JSON.parse(localStorage.getItem('sz_products')) || defaultProducts;
        let orders = JSON.parse(localStorage.getItem('sz_orders')) || [];
        let cart = [];
        let activeCategory = "all";

        function saveSettings() { localStorage.setItem('sz_settings', JSON.stringify(siteSettings)); renderBranding(); }
        function saveAdmins() { localStorage.setItem('sz_admin_emails', JSON.stringify(adminEmails)); renderAdminEmails(); }
        function saveCategories() { localStorage.setItem('sz_categories', JSON.stringify(categories)); renderCategories(); updateAdminCategoryDropdown(); renderAdminSegments(); }
        function saveProducts() { localStorage.setItem('sz_products', JSON.stringify(products)); renderProducts(); renderAdminProducts(); }
        function saveOrders() { localStorage.setItem('sz_orders', JSON.stringify(orders)); renderAdminOrders(); }

        function fileToBase64(file) {
            return new Promise((resolve, reject) => {
                const reader = new FileReader();
                reader.readAsDataURL(file);
                reader.onload = () => resolve(reader.result);
                reader.onerror = error => reject(error);
            });
        }

        function renderBranding() {
            document.getElementById('site-logo-text').innerHTML = siteSettings.logoText;
            document.getElementById('banner-title').innerText = siteSettings.bannerTitle;
            document.getElementById('banner-subtitle').innerText = siteSettings.bannerSubtitle;
            document.getElementById('hero-banner').style.backgroundImage = `linear-gradient(rgba(0,0,0,0.75), rgba(0,0,0,0.85)), url('${siteSettings.bannerBg}')`;
            document.getElementById('display-bkash-no').innerText = siteSettings.whatsappNo;
            document.getElementById('footer-wa-no').innerText = siteSettings.whatsappNo;

            const profileImgElem = document.getElementById('site-profile-img');
            if(siteSettings.profileImg) {
                profileImgElem.src = siteSettings.profileImg;
                profileImgElem.classList.remove('hidden');
            } else {
                profileImgElem.classList.add('hidden');
            }

            document.getElementById('cms-logo-text').value = siteSettings.logoText;
            document.getElementById('cms-whatsapp-no').value = siteSettings.whatsappNo;
            document.getElementById('cms-banner-title').value = siteSettings.bannerTitle;
            document.getElementById('cms-banner-subtitle').value = siteSettings.bannerSubtitle;
        }

        function loginUser() {
            const email = prompt("Enter your Gmail address to sign in:");
            if(email && email.trim() !== '') {
                currentUser = { email: email.toLowerCase().trim() };
                localStorage.setItem('sz_current_user', JSON.stringify(currentUser));
                updateAuthUI();
            }
        }

        function logoutUser() {
            currentUser = null;
            localStorage.removeItem('sz_current_user');
            updateAuthUI();
        }

        function updateAuthUI() {
            const authSec = document.getElementById('auth-section');
            const adminBtn = document.getElementById('admin-btn');

            if(currentUser) {
                const isAdmin = adminEmails.includes(currentUser.email);
                authSec.innerHTML = `
                    <div class="flex items-center gap-2">
                        <span class="text-xs text-yellow-400 font-bold bg-gray-800 px-2 py-1 rounded border border-gray-700">${currentUser.email.split('@')[0]}</span>
                        <button onclick="logoutUser()" class="text-xs bg-gray-800 border border-gray-700 hover:bg-red-900/40 text-red-400 px-2 py-1 rounded">Logout</button>
                    </div>
                `;
                
                if(isAdmin) {
                    adminBtn.classList.remove('hidden');
                    document.getElementById('admin-logged-email').innerText = currentUser.email;
                } else {
                    adminBtn.classList.add('hidden');
                }
            } else {
                authSec.innerHTML = `
                    <button onclick="loginUser()" class="bg-blue-600 hover:bg-blue-700 text-white font-bold px-3 py-1.5 rounded-lg text-sm transition flex items-center gap-1.5">
                        <i class="fa-brands fa-google"></i> Sign In
                    </button>
                `;
                adminBtn.classList.add('hidden');
            }
        }

        function renderCategories() {
            const container = document.getElementById('category-container');
            container.innerHTML = categories.map(cat => `
                <button onclick="filterCategory('${cat.id}')" 
                    class="px-4 py-1.5 rounded-full text-sm font-semibold whitespace-nowrap border transition ${activeCategory === cat.id ? 'bg-yellow-400 text-gray-950 border-yellow-400' : 'bg-gray-800 text-gray-300 border-gray-700 hover:border-yellow-400'}">
                    ${cat.name}
                </button>
            `).join('');
        }

        function filterCategory(catId) {
            activeCategory = catId;
            renderCategories();
            const catObj = categories.find(c => c.id === catId);
            document.getElementById('current-category-title').innerText = catObj ? catObj.name : 'Products';
            renderProducts();
        }

        function renderProducts() {
            const grid = document.getElementById('product-grid');
            const filtered = activeCategory === "all" ? products : products.filter(p => p.category === activeCategory);

            document.getElementById('product-count-tag').innerText = `${filtered.length} Products`;

            if(filtered.length === 0) {
                grid.innerHTML = `<div class="col-span-full text-center py-16 text-gray-500">No products added in this segment yet.</div>`;
                return;
            }

            grid.innerHTML = filtered.map(p => {
                const hasOffer = p.offerPrice && p.offerPrice < p.price;
                return `
                <div class="bg-gray-900 rounded-xl border border-gray-800 overflow-hidden flex flex-col justify-between hover:border-yellow-400/50 transition shadow-lg">
                    <div>
                        <div class="relative h-48 bg-gray-950 overflow-hidden">
                            <img src="${p.image}" alt="${p.name}" class="w-full h-full object-cover">
                            ${p.tag ? `<span class="absolute top-2 left-2 bg-yellow-400 text-gray-950 text-[10px] font-black px-2 py-0.5 rounded shadow">${p.tag}</span>` : ''}
                            ${hasOffer ? `<span class="absolute top-2 right-2 bg-red-600 text-white text-[10px] font-bold px-2 py-0.5 rounded">OFFER</span>` : ''}
                        </div>
                        <div class="p-4">
                            <span class="text-[10px] text-yellow-400 font-extrabold uppercase tracking-widest bg-yellow-400/10 border border-yellow-400/20 px-2 py-0.5 rounded">${p.category}</span>
                            <h3 class="font-bold text-lg text-white mt-2">${p.name}</h3>
                            <div class="mt-2 flex items-baseline gap-2">
                                <span class="text-xl font-extrabold text-white">৳${hasOffer ? p.offerPrice : p.price}</span>
                                ${hasOffer ? `<span class="text-sm text-gray-500 line-through">৳${p.price}</span>` : ''}
                            </div>
                        </div>
                    </div>
                    <div class="p-4 pt-0">
                        <button onclick="addToCart(${p.id})" class="w-full bg-yellow-400 hover:bg-yellow-500 text-gray-950 font-bold py-2.5 rounded-lg transition flex items-center justify-center gap-2">
                            <i class="fa-solid fa-cart-plus"></i> Add to Cart
                        </button>
                    </div>
                </div>
            `}).join('');
        }

        function toggleCart() { document.getElementById('cart-modal').classList.toggle('hidden'); }

        function addToCart(id) {
            const product = products.find(p => p.id === id);
            const existing = cart.find(item => item.id === id);
            if(existing) existing.qty++;
            else cart.push({ ...product, qty: 1 });
            updateCartUI();
            toggleCart();
        }

        function updateCartQty(id, change) {
            const item = cart.find(i => i.id === id);
            if(item) {
                item.qty += change;
                if(item.qty <= 0) cart = cart.filter(i => i.id !== id);
            }
            updateCartUI();
        }

        function updateCartUI() {
            document.getElementById('cart-count').innerText = cart.reduce((sum, i) => sum + i.qty, 0);
            const itemsContainer = document.getElementById('cart-items');
            
            if(cart.length === 0) {
                itemsContainer.innerHTML = `<p class="text-center text-gray-500 py-6">Your cart is empty.</p>`;
                document.getElementById('cart-total').innerText = '৳0';
                return;
            }

            let total = 0;
            itemsContainer.innerHTML = cart.map(item => {
                const finalPrice = item.offerPrice || item.price;
                const itemTotal = finalPrice * item.qty;
                total += itemTotal;
                return `
                    <div class="flex items-center justify-between bg-gray-800/50 p-3 rounded-lg border border-gray-800">
                        <div class="flex items-center gap-3">
                            <img src="${item.image}" class="w-12 h-12 object-cover rounded">
                            <div>
                                <h4 class="font-bold text-sm text-white">${item.name}</h4>
                                <p class="text-xs text-yellow-400">৳${finalPrice} × ${item.qty}</p>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <button onclick="updateCartQty(${item.id}, -1)" class="bg-gray-700 px-2 rounded text-white font-bold">-</button>
                            <span class="text-sm font-bold">${item.qty}</span>
                            <button onclick="updateCartQty(${item.id}, 1)" class="bg-gray-700 px-2 rounded text-white font-bold">+</button>
                        </div>
                    </div>
                `;
            }).join('');

            document.getElementById('cart-total').innerText = `৳${total}`;
        }

        function togglePaymentDetails() {
            const method = document.querySelector('input[name="payment-method"]:checked').value;
            const bkashInfo = document.getElementById('bkash-info');
            const bkashTrx = document.getElementById('bkash-trx');
            if(method === 'bKash') {
                bkashInfo.classList.remove('hidden');
                bkashTrx.required = true;
            } else {
                bkashInfo.classList.add('hidden');
                bkashTrx.required = false;
            }
        }

        function handleCheckout(e) {
            e.preventDefault();
            if(cart.length === 0) return alert('Cart is empty!');

            const name = document.getElementById('cust-name').value;
            const phone = document.getElementById('cust-phone').value;
            const address = document.getElementById('cust-address').value;
            const method = document.querySelector('input[name="payment-method"]:checked').value;
            const trxId = method === 'bKash' ? document.getElementById('bkash-trx').value : 'N/A';
            
            let totalAmount = 0;
            let productDetailsList = "";

            cart.forEach((item, index) => {
                const price = item.offerPrice || item.price;
                const itemTotal = price * item.qty;
                totalAmount += itemTotal;
                productDetailsList += `${index + 1}. ${item.name} - ${item.qty}x = ৳${itemTotal}\n`;
            });

            const newOrder = {
                id: Date.now(),
                date: new Date().toLocaleString(),
                name, phone, address, method, trxId, totalAmount, items: cart
            };

            orders.unshift(newOrder);
            saveOrders();

            let message = `🛒 *NEW ORDER ON SPORTIVEX!*\n\n`;
            message += `👤 *Customer Info:*\n• Name: ${name}\n• Phone: ${phone}\n• Address: ${address}\n\n`;
            message += `🛍️ *Items Ordered:*\n${productDetailsList}\n`;
            message += `💰 *Total Amount:* ৳${totalAmount}\n`;
            message += `💳 *Payment Method:* ${method}\n`;
            if(method === 'bKash') message += `🔑 *bKash TrxID:* ${trxId}\n`;

            const cleanWANumber = siteSettings.whatsappNo.replace(/[^0-9]/g, '');
            const targetPhone = cleanWANumber.startsWith('88') ? cleanWANumber : `88${cleanWANumber}`;
            const whatsappURL = `https://wa.me/${targetPhone}?text=${encodeURIComponent(message)}`;
            
            cart = [];
            updateCartUI();
            toggleCart();
            document.getElementById('checkout-form').reset();

            window.open(whatsappURL, '_blank');
        }

        function openAdminModal() {
            if(!currentUser || !adminEmails.includes(currentUser.email)) {
                return alert("Access Denied: Only authorized admin emails can open CMS.");
            }
            document.getElementById('admin-modal').classList.remove('hidden');
            renderAdminEmails();
            renderAdminSegments();
            renderAdminProducts();
            renderAdminOrders();
            updateAdminCategoryDropdown();
        }

        function closeAdminModal() { document.getElementById('admin-modal').classList.add('hidden'); }

        function switchAdminTab(tabName) {
            ['site-settings', 'add-product', 'manage-products', 'manage-segments', 'manage-admins', 'view-orders'].forEach(tab => {
                document.getElementById(`tab-${tab}`).classList.add('hidden');
            });
            ['settings', 'add', 'manage', 'segments', 'admins', 'orders'].forEach(btn => {
                const elem = document.getElementById(`tab-btn-${btn}`);
                if(elem) elem.className = "py-2 px-3 text-sm font-bold text-gray-400 hover:text-white whitespace-nowrap";
            });

            document.getElementById(`tab-${tabName}`).classList.remove('hidden');
            const activeBtn = document.getElementById(`tab-btn-${tabName.split('-')[1] || tabName.split('-')[0]}`);
            if(activeBtn) activeBtn.className = "py-2 px-3 text-sm font-bold border-b-2 border-yellow-400 text-yellow-400 whitespace-nowrap";
        }

        async function handleSaveSiteSettings(e) {
            e.preventDefault();
            
            siteSettings.logoText = document.getElementById('cms-logo-text').value;
            siteSettings.whatsappNo = document.getElementById('cms-whatsapp-no').value;
            siteSettings.bannerTitle = document.getElementById('cms-banner-title').value;
            siteSettings.bannerSubtitle = document.getElementById('cms-banner-subtitle').value;

            const profileFile = document.getElementById('cms-profile-file').files[0];
            if(profileFile) {
                siteSettings.profileImg = await fileToBase64(profileFile);
            }

            const bannerFile = document.getElementById('cms-banner-file').files[0];
            if(bannerFile) {
                siteSettings.bannerBg = await fileToBase64(bannerFile);
            }

            saveSettings();
            alert('Site branding & files saved successfully!');
        }

        async function handleAddProduct(e) {
            e.preventDefault();
            const name = document.getElementById('prod-name').value;
            const price = parseFloat(document.getElementById('prod-price').value);
            const offerPrice = document.getElementById('prod-offer').value ? parseFloat(document.getElementById('prod-offer').value) : null;
            const tag = document.getElementById('prod-tag').value.trim();
            const category = document.getElementById('prod-category').value;
            const imageFile = document.getElementById('prod-image-file').files[0];

            if(!imageFile) return alert('Please upload a product image file.');

            const imageBase64 = await fileToBase64(imageFile);

            products.unshift({ id: Date.now(), name, price, offerPrice, tag, category, image: imageBase64 });
            saveProducts();
            e.target.reset();
            alert('New product published successfully!');
            switchAdminTab('manage-products');
        }

        function deleteProduct(id) {
            if(confirm('Delete product permanently?')) {
                products = products.filter(p => p.id !== id);
                saveProducts();
            }
        }

        function renderAdminProducts() {
            document.getElementById('admin-product-list').innerHTML = products.map(p => `
                <div class="py-3 flex items-center justify-between">
                    <div class="flex items-center gap-3">
                        <img src="${p.image}" class="w-12 h-12 object-cover rounded border border-gray-700">
                        <div>
                            <h4 class="font-bold text-sm text-white">${p.name}</h4>
                            <p class="text-xs text-gray-400">Regular: ৳${p.price} | Offer: ৳${p.offerPrice || 'N/A'} | Tag: ${p.tag || 'None'} | Category: ${p.category}</p>
                        </div>
                    </div>
                    <button onclick="deleteProduct(${p.id})" class="text-red-500 hover:text-red-400 p-2"><i class="fa-solid fa-trash"></i></button>
                </div>
            `).join('');
        }

        function handleAddSegment(e) {
            e.preventDefault();
            const id = document.getElementById('seg-id').value.toLowerCase().trim();
            const name = document.getElementById('seg-name').value.trim();
            if(categories.some(c => c.id === id)) return alert('Category ID already exists!');
            categories.push({ id, name });
            saveCategories();
            document.getElementById('seg-id').value = '';
            document.getElementById('seg-name').value = '';
        }

        function deleteSegment(id) {
            if(id === 'all') return alert('Cannot delete default category!');
            if(confirm(`Delete category "${id}"?`)) {
                categories = categories.filter(c => c.id !== id);
                saveCategories();
            }
        }

        function renderAdminSegments() {
            document.getElementById('admin-segments-list').innerHTML = categories.filter(c => c.id !== 'all').map(c => `
                <div class="flex justify-between items-center bg-gray-800 border border-gray-700 p-2.5 rounded">
                    <span class="text-sm font-bold text-yellow-400">${c.name} <span class="text-xs text-gray-400 font-normal">(${c.id})</span></span>
                    <button onclick="deleteSegment('${c.id}')" class="text-xs text-red-400 hover:text-red-300 font-bold"><i class="fa-solid fa-trash"></i> Delete</button>
                </div>
            `).join('');
        }

        function updateAdminCategoryDropdown() {
            document.getElementById('prod-category').innerHTML = categories.filter(c => c.id !== 'all').map(c => `
                <option value="${c.id}">${c.name}</option>
            `).join('');
        }

        function handleAddAdminEmail(e) {
            e.preventDefault();
            const email = document.getElementById('new-admin-email').value.toLowerCase().trim();
            if(!adminEmails.includes(email)) {
                adminEmails.push(email);
                saveAdmins();
                document.getElementById('new-admin-email').value = '';
                alert(`Admin access granted to ${email}`);
            }
        }

        function removeAdminEmail(email) {
            if(adminEmails.length <= 1) return alert('At least one admin email must exist!');
            if(confirm(`Revoke admin access for ${email}?`)) {
                adminEmails = adminEmails.filter(e => e !== email);
                saveAdmins();
            }
        }

        function renderAdminEmails() {
            document.getElementById('admin-emails-list').innerHTML = adminEmails.map(email => `
                <div class="flex justify-between items-center bg-gray-800 border border-gray-700 p-2.5 rounded">
                    <span class="text-sm font-semibold text-gray-200">${email}</span>
                    <button onclick="removeAdminEmail('${email}')" class="text-xs text-red-400 hover:text-red-300 font-bold">Remove</button>
                </div>
            `).join('');
        }

        function renderAdminOrders() {
            const list = document.getElementById('admin-order-list');
            if(orders.length === 0) {
                list.innerHTML = `<p class="text-center text-gray-500 py-6">No order records found.</p>`;
                return;
            }

            list.innerHTML = orders.map(o => `
                <div class="bg-gray-800 border border-gray-700 p-4 rounded-lg space-y-2">
                    <div class="flex justify-between items-start border-b border-gray-700 pb-2">
                        <div>
                            <h4 class="font-bold text-yellow-400">${o.name} (${o.phone})</h4>
                            <p class="text-xs text-gray-400">${o.date}</p>
                        </div>
                        <span class="bg-green-900/40 border border-green-700 text-green-300 text-xs px-2 py-0.5 rounded font-bold">${o.method}</span>
                    </div>
                    <p class="text-xs text-gray-300"><strong>Address:</strong> ${o.address}</p>
                    ${o.method === 'bKash' ? `<p class="text-xs text-pink-400"><strong>bKash TrxID:</strong> ${o.trxId}</p>` : ''}
                    <div class="text-xs border-t border-gray-700 pt-2 space-y-1">
                        <strong>Ordered Items:</strong>
                        ${o.items.map(i => `<div class="flex justify-between text-gray-400"><span>• ${i.name} × ${i.qty}</span><span>৳${(i.offerPrice || i.price) * i.qty}</span></div>`).join('')}
                    </div>
                    <div class="text-right font-bold text-sm text-yellow-400 border-t border-gray-700 pt-1">
                        Total Amount: ৳${o.totalAmount}
                    </div>
                </div>
            `).join('');
        }

        renderBranding();
        renderCategories();
        renderProducts();
        updateAdminCategoryDropdown();
        updateAuthUI();
    </script>
</body>
</html>
